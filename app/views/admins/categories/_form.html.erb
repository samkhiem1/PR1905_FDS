<% provide(:title, 'Category') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
  <%= form_for(@category, url: links) do |f| %>

      <%= render "shared/error_messages", object: f.object %>
      <%= f.label :Tên %>
      <%= f.text_field :name, class: 'form-control category-form'%>
      <%= f.submit yield(:button_text), class: "btn btn-primary coin" %>
    <% end %>
  </div>
</div>

