<div class="row" style="row=5 ">
  <div class="col-md-6 col-md-offset-3 form-group">
    <%= form_for [@document, @document.comments.build ], remote: true do |f|%>
      <%= f.text_field :content, class: 'form-control form-group comments' %>
      <%= f.submit class: "btn btn-primary form-group" %>
    <% end %>
  </div>
</div>
